# MirrorFit

A mobile-first, fullscreen “smart mirror” web app that opens with two quick prompts (mood + destination) and then launches a front-camera mirror view with minimal UI, automatically overlaying outfit looks that can be swiped through, regenerated, and saved to a closet gallery.

Generated by deployRocket.

## Generation Status

Application files have not been generated yet. The latest deployRocket run failed before Codex returned a valid file set.

Current stage: Continuing input processing

Latest error: none

Live site: pending GitHub Pages publish

Project state lives on the `deployrocket-state` branch in `README.md`.
